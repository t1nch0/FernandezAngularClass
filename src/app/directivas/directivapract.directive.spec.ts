import { DirectivapractDirective } from './directivapract.directive';

describe('DirectivapractDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivapractDirective();
    expect(directive).toBeTruthy();
  });
});
